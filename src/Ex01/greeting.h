#ifndef greeting_h
#define greeting_h

/**
 * @brief Get a string to greet the user with. The string returned must be freed by the caller.
 *
 * @return char* A string to greet the user.
 */
char* greeting();

#endif